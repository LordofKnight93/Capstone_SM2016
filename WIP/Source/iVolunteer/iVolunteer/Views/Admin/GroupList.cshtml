@model IEnumerable<iVolunteer.Models.Data_Definition_Class.MongoDB.CollectionClass.Mongo_Group>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>Group Name</th>
        <th>Member</th>
        <th>Staus</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>@Html.DisplayFor(modelItem => item.GroupInformation.GroupName)</td>
        <td>@Html.DisplayFor(modelItem => item.GroupInformation.MemberCount)</td>
        <td>@Html.DisplayFor(modelItem => item.GroupInformation.IsActivate)</td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
