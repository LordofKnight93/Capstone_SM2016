using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace iVolunteer.Common
{
    public static class ValidationHelper
    {
        // Check if string is email or not
        public static bool IsEmail(string email)
        {
            return true;
        }
        // Check if password if valid or not
        public static bool IsValidPassword(string password)
        {
            return true;
        }
        // Check if string is identifyID or not
        public static bool IsIdentifyID(string indentifyID)
        {
            return true;
        }
    }
}